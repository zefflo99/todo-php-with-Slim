<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="styles.css">
    <title>Todo List</title>
</head>
<body>
<div class="container mt-5">
    <div class="card todo-container">
        <h1 class="card-header text-center text-primary">Welcome to my Todo list</h1>

        <form action="/" method="post" class="card-body">
            <div class="mb-3">
                <label for="todo" class="form-label">Todo:</label>
                <input type="text" id="todo" name="todo" class="form-control" placeholder="Enter a todo" required>
            </div>
            <button type="submit" name="submitTodo" class="btn btn-success">Add Todo</button>

        </form>
        <form action="/deleteAll" method="post" class="card-body">
            <button type="submit" name="deleteAll" class="btn btn-success">Reset ALL</button>
        </form>


        <form action="/sort" method="get" class="card-body">
            <button type="submit" class="btn btn-primary">A-Z</button>
        </form>


        <ul class="list-group list-group-flush">
            {% for todo in todos %}
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <form action="/edit" method="post" class="d-flex flex-row gap-1">
                        <input class="form-check-input edit-checkbox" type="checkbox" name="editCheckbox" id="todo{{ todo.id }}">
                        <input type="submit" name="todoId" value="{{ todo.id }}" id="confirmEdit{{ todo.id }}" class="d-none">
                        <label class="form-check-label" for="todo{{ todo.id }}">
                            <input type="text" class="form-control edit-input" name="editTodo" value="{{ todo.name }}">
                        </label>
                    </form>
                    <div class="edit-buttons">
                        <label for="confirmEdit{{ todo.id }}" class="btn btn-success btn-sm ms-2 confirm-edit">Edit</label>
                        <form action="/delete" method="post" class="edit-form">
                            <button type="submit" class="btn btn-danger btn-sm ms-2 delete-button" name="todoId" value="{{ todo.id }}">
                                Delete
                            </button>
                        </form>
                    </div>
                </li>
            {% endfor %}
        </ul>



    </div>
</div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>
